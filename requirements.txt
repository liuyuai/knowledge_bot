# Web 框架
fastapi>=0.110.0
uvicorn>=0.29.0

# HTTP 客户端（调用大模型 API）
httpx>=0.27.0

# 向量数据库
chromadb>=0.4.0

# BM25 关键词检索
rank_bm25>=0.2.2

# 文档解析（可选，ingest.py 中按需导入）
python-docx>=1.1.0
pypdf>=4.0.0

# Redis 会话存储（可选，不配置 REDIS_URL 时自动回退内存）
redis>=5.0.0
